name: aaspas
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^9.0.2
  uuid: ^4.0.0
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.1
  geocoding: ^2.1.0
  image_picker: ^1.0.0
  firebase_storage: ^11.6.0
  firebase_auth: ^4.17.0
  firebase_messaging: ^14.7.0   # FCM - Real push notifications
  shared_preferences: ^2.2.2    # Local caching
  share_plus: ^7.2.2            # Share events
  http: ^1.1.0
  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  firebase_app_check: ^0.2.0
  google_sign_in: ^6.1.0
  js: ^0.7.1
  web: ^0.5.1
  test: ^1.24.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true